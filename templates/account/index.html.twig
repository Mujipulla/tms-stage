{% extends 'base.html.twig' %}

{% block title %}Mon compte : Boutique poles{% endblock %}

{% block body %}
    <div class="container" style="margin-top: 100px">
        <h1>Mon compte</h1>
        <div class="row">
            <div class="col-md-3">
                {% include '_partials/aside.html.twig' %}
            </div>
            <div class="col-md-9">
                <p>
                    Bienvenu <span class="w3-text-blue">{{ app.user.firstname }} {{ app.user.lastname }}</span> dans votre espace membre.<br>
                    Vous pouvez modifier vos informations personnelles en cliquant sur le bouton ci-dessous.
                    <a  class="w3-text-blue" href="{{ path('app_logout') }}">Déconnexion</a>
                </p>
            </div>
        </div>
    </hr>
{% endblock %}